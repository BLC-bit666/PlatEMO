请读取附件 README_FIRST.md、LATEST_SYNTHESIS.md 和两轮 REPORT.md，再结合代码、CSV、生成图与数值快照独立诊断。

**首要问题：后期 CGAN 跟不上搜索进展，生成结果与当前训练集明显偏离。请先找清原因，再提出最小修改。**

目标是按参考方向生成有价值的候选，引导或加速 P1 探索边界，尤其尚未探索的部分；不要求候选本身可行。可行/不可行条件 s 是否保留可以讨论，但不要与判别器的真实/生成区分混淆。

已有证据：固定尺度＋去 s 的组合在独立种子上，后半程 IGD 指标反而变差 9.5%；冻结图中晚期数据后，24 组续训/完全重置对照各训练 10000 次 G 更新，仍未统一消除偏移；修正 D 的方向错配评分后，两题生成距离反而增大 22.7%/24.6%；简单加宽也未统一解决。请核对这些结论的适用范围，不把失败扩大为所有同类方案均无效。

**存档是否有问题仍未排除。** 固定数据重置失败，不代表数据合理。请核查“候选池与全局第一层筛选→支配过滤与配对→激活、去重、采样→方向/0–1 条件→G/D 训练→候选使用”的对应关系，区分实现错误、训练分布不适合当前模型、优化失败和使用失效。重点检查有效样本覆盖、同条件的决策分布，以及当前零噪声生成的影响；不要预设其中任何一项是根因。

原则：保持一个 G、一个 D，不加辅助网络；不将真实解坐标作为模型上下文；不大改 P1/P2、存档和评价预算的主流程。**优先统一和收敛，避免分支和堆料。** 存档规则若需调整，必须有直接证据并限定为局部修改。

请聚焦证据最强的一个主因，引用具体代码、数据和图，说明因果证据及尚存盲区；证据不足就明确说。然后给出一个最小、可证伪的区分实验及成功/失败判据，再提出必要修改。以改善后期生成并最终提高 P1 边界探索为目标，不以可行率、训练误差或单张好图代替搜索价值。
