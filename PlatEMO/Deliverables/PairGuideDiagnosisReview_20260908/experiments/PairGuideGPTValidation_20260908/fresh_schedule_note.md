固定条件与变动条件的六对批次序列已逐一验证完全相同，实际序列另存 batch_schedules/*.csv。首次新初始化诊断中，按 seed 重新构造序列时，4 个案例因 worker 默认 RNG 类型与历史上下文不同，被批次一致性断言阻止；均未进入训练或离线评价。两个通过断言的 seed1 案例保留。随后所有诊断直接读取已冻结的实际批次序列；每个训练事件仍显式使用相同 twister RNG。完整搜索从一开始就显式设定 twister，不受影响。
