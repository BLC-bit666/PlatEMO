# 修正存档：LIRCMOP5_BC～8_BC，3 runs

已完成并核验：12/12 次运行，0 失败；72 条检查点记录，48 张分布图（仅 run 1）和 4 张 IGD/HV 汇总曲线。每次 N=100、D=30、FE=100000，首次真实使用均有记录。86 个活动源码及其快照的 SHA-256 均与启动清单一致。

| 问题 | 最终 IGD（均值 ± 样本标准差） | 最终 HV（均值 ± 样本标准差） |
| --- | ---: | ---: |
| LIRCMOP5_BC | 0.021447 ± 0.014110 | 0.283823 ± 0.006524 |
| LIRCMOP6_BC | 0.015694 ± 0.004180 | 0.192241 ± 0.001368 |
| LIRCMOP7_BC | 0.008779 ± 0.000651 | 0.293401 ± 0.000562 |
| LIRCMOP8_BC | 0.007701 ± 0.000270 | 0.294077 ± 0.000206 |

图像已抽查四题及指标曲线。LIRCMOP6_BC run 1 在 FE=100000 时存档为空、没有对应生成事件，该节点保留实际种群并明确标记缺失。其他 23 个绘图节点均核验训练、生成、消费时序和实际使用点一致。部分生成分布仍可见偏移；本批未调整网络或训练参数。

详细结果见 analysis/problem_summary.csv、analysis/checkpoints_per_run.csv；核验记录见 verification.json。

4 个问题 × seed 1:3，仅 CGAN，共 12 次。N=100，默认 D=30，maxFE=100000；10 个工作进程，每进程单线程。首训/重训保持 200/20 次生成器更新，最多 16 个完整 pair 一个 batch，Critic:G=5:1，模型持续训练。

存档先对完整候选可行集合统一分层，默认第一层；再排除被任一候选可行点支配的不可行端，之后在原五方向邻域内配对。同一不可行端可复用。不合格配对删除，最多 500 对，允许不足或为空。P1/P2 保持 CCMO 选择池，存档仍接收完整 Union。

观测节点为首次真实 CGAN 使用完成环境选择之后，以及 FE=10000、30000、50000、70000、100000。仅 seed=1 绘图，每节点全景及 focus 各一张。左图为该批模型实际训练时的 P1/P2 与存档；右图为原生候选及原样实际使用的筛选解。时刻分别标注；无对应事件时明确缺失，不新增训练凑图。

- 运行状态：state.mat、campaign.log、各 *.progress.txt 和 *.checkpoints.csv。
- 结果：12 个运行 MAT；汇总已保存至 analysis/。
- 分布图：analysis/figures/<problem>/run_01/；四题共 48 张（六节点 × 全景/focus）。
- plot_data.mat 保存图中训练、生成、消费与观测状态。离线评价独立记账，不反馈搜索 FE。
- configuration.json、protocol.mat、source_manifest.json 固定配置和源码；source_snapshot 仅供审计，不得加入 MATLAB 活动路径。

本批复用上轮已通过的六项回归、30 个真实状态重建和静态检查；启动前确认其对应源码未变化。运行时使用新目录，保留全部旧实验结果。

启动入口：本目录 run_campaign.m。该批已完成，无需重复启动。
