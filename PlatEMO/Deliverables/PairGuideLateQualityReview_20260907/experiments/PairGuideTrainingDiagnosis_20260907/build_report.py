from pathlib import Path
import csv

OUT = Path(__file__).resolve().parent
ROOT = OUT.parent.parent
CAMPAIGN = ROOT / 'Data/PairGuideFilteredArchive_5to8_R3_20260907'

def read(path):
    with path.open() as f:
        return list(csv.DictReader(f))

models = read(OUT / 'model_summary.csv')
assert len(models) == 48
lookup = {(r['problem'], r['FE'], r['arm']): r for r in models}
def row(n, arm, fe=99800):
    return lookup[(f'LIRCMOP{n}_BC', str(fe), arm)]

def table(arms, objective=False):
    result = ['| 固定训练集对照 | 问题 7 | 问题 8 |', '| --- | ---: | ---: |']
    key = 'midpointObjectiveRMSE' if objective else 'RMSE'
    for name, arm in arms:
        result.append(f"| {name} | {float(row(7, arm)[key]):.6f} | {float(row(8, arm)[key]):.6f} |")
    return '\n'.join(result)

budget_arms = [
    ('原模型，原预算 20 步', 'production20'), ('保留模型，1000 步', 'warm1000'),
    ('保留模型，去掉对抗项，1000 步', 'noAdv1000'),
    ('保留模型，均衡 gap 权重，1000 步', 'uniformGap1000'),
    ('保留模型，仅 G 学习率增至 1e-3，1000 步', 'lr1e3_1000'),
    ('保留模型，训练/生成噪声置零，1000 步', 'noNoise1000'),
    ('保留模型，5000 步', 'warm5000')]
structure_arms = [
    ('原参考向量，32×32，原损失', 'fresh32_5000'),
    ('原参考向量，128×128，原损失', 'fresh128_5000'),
    ('方向 one-hot，32×32，原损失', 'onehot32_5000'),
    ('原参考向量，32×32，均衡 gap 权重', 'fresh32uniform5000'),
    ('原参考向量，128×128，均衡 gap 权重', 'fresh128uniform5000'),
    ('方向 one-hot，32×32，均衡 gap 权重', 'onehot32uniform5000')]

result_rows = read(CAMPAIGN / 'analysis/problem_summary.csv')
run_rows = read(CAMPAIGN / 'interval_summary.csv')
summary = ['| 问题 | 最终 IGD，均值 ± 标准差 | CGAN 子代当代进入 P1 | 训练占搜索运行时间 |', '| --- | ---: | ---: | ---: |']
for r in result_rows:
    runs = [x for x in run_rows if x['problem'] == r['problem']]
    guided = sum(float(x['guideFull']) for x in runs)
    survived = sum(float(x['survivedP1']) * float(x['guideFull']) for x in runs) / guided
    time_share = sum(float(x['trainingSeconds']) for x in runs) / sum(float(x['wallSeconds']) for x in runs)
    summary.append(f"| {r['problem']} | {float(r['finalIGDMean']):.6f} ± {float(r['finalIGDStd']):.6f} | {survived:.2%} | {time_share:.1%} |")

text = f'''# PairGuide 训练偏移：修正存档后的诊断

**主要结论：当前网络没有分别学会各方向的真实端点，而是在不同方向、不同配对之间做了不合适的折中。最直接的设计因素是过于简化的方向条件与全局共享的绝对坐标回归；`1/gap²` 的极端权重进一步让少数配对支配训练。持续训练本身不是此次数据支持的主要原因，单独增加更新数、去掉对抗项或加宽网络均没有解决问题。**

这份结论包含一个限制：改用更易区分方向的编码，确实大幅改善决策端点拟合，但还没有得到足够好的目标空间对齐和可行端点标签。因此本轮给出了原因排序，没有把任何诊断变体升级为生产修复。

**证据范围与方法**

使用修正存档后的 LIRCMOP5_BC～8_BC、各 3 runs 正式结果；核对 72 条检查点、24 个绘图节点的数据，其中 1 个节点没有生成事件。抽查原始图，并生成、检查新的诊断图。

完整读取四题 seed 1 的 1501 次成功训练记录。对问题 7、8 精确重放分别 466、467 次成功训练，每次固定诊断 RMSE 与原记录相差小于 `1e-9`。随后冻结 FE=99800 的真实训练集、训练前模型和随机状态，做单变量和交叉对照；另验证问题 7 的首次训练与 FE=30000 状态，共保存 48 个诊断模型结果。没有修改生产模型、正式实验或搜索参数。

主要 RMSE 表沿用生产日志的固定噪声探针，单位是归一化决策变量范围。额外生成每条件 64 组共同噪声，用于偏差/噪声分解和真实目标、约束测量。所有比较都使用匹配条件、同一 alpha 的真实端点插值作为候选目标参照；不把未匹配方向硬配到旧训练点。

**1. 实验收敛不代表 CGAN 已学好**

{chr(10).join(summary)}

进入 P1 的比例是所有已真实评价的 CGAN 子代在当代环境选择后的保留率，三次运行按子代数加权。它不是后代的全部间接贡献。当前批次只有 CGAN 模式，不能从这些数值断言 CGAN 相比 fallback-only 的净收益，但足以说明其直接使用效率较低、计算代价较高。

偏移也不限于 seed 1：问题 7 三个 run 最后一次训练 RMSE 为 0.1250、0.0968、0.1401；问题 8 为 0.0907、0.0885、0.0728。

**2. 从图像中区分三个阶段**

首次真实使用时，四题首训 RMSE 约为 0.213～0.242，生成云明显远离实际训练端点，部分目标值达到十几至二十多。此时模型没有训练历史，已经出现偏移，因而不能归因于继续训练积累了旧模型污染。高可行率也不表示正确：远离边界、目标很差的区域可能仍然可行。

问题 7 在 FE=30000 的图中，生成云落在训练边界的下方，生成端点本身已经不匹配。问题 7、8 末期仍有这一问题，且该批生成前刚刚训练，所有查询方向都在对应训练集中；它们适合隔离训练本身的原因。

问题 5、6 后期还叠加了服务时效问题。问题 5 在 FE=100000 的候选使用 FE=74600 训练的模型；生成时距训练已过 25200 FE，只有 50% 的查询方向出现在该次训练集中。问题 6 的 FE=70000 节点相应比例为 43%；FE=100000 存档为空，没有生成事件。已训练模型可在少于 8 对时服务、但不能重训，是当前明确保留的规则。它解释这两题的额外偏移，不能解释问题 7、8 刚训练完仍然偏移的情况。

图中 FE=30000 的已消费模型训练于 FE=29800；独立的 30000 对照冻结的是 FE=30000 的训练事件。两者是相邻事件，没有混为同一个模型。

**3. 偏移主要发生在端点生成阶段**

在实际图像对应的 500 个原生候选中，问题 7、8 末期的生成端点目标 RMSE 分别为 0.243、0.267。把网络端点替换成完全正确的真实端点后，纯粹由目标函数非线性造成的插值偏差分别约为 0.041、0.048。后者指 `f((1-a)xf+a·xi)` 与 `(1-a)f(xf)+a·f(xi)` 的差异。两项不是可直接相加的误差分解，但量级说明插值解释不了主要差距。

真实模型固定探针中，问题 7 的生成均值偏差占总 MSE 的 {float(row(7,'production20')['biasMSEShare']):.2%}，问题 8 为 {float(row(8,'production20')['biasMSEShare']):.2%}。原生云的位置本身有偏差，增加采样数量只会更密地采到这片偏移区域。

问题 7 的真实平均配对间隙为 0.1145，生成均值间隙只有 0.0272；问题 8 相应为 0.1047、0.0451。生成配对方向与真实方向的平均余弦仅约 0.016、0.037。网络不仅没学准端点位置，也没学准每个条件的配对方向；给两端共享噪声不能自动补上这些信息。

**4. 条件表示是最值得优先处理的因素**

生产输入为 `[z, w1, w2, side]`，G 为 32×32、输出 30 维绝对决策。每个方向只有一对活跃训练端点，因此单次训练集没有同条件多标签的逻辑矛盾；足够灵活的函数可以记住这些点。

难点是实际映射并不平滑。问题 7、8 末期相邻方向的真实端点跳变量中位数约为 0.0699、0.0672；原网络生成均值的跳变量仅约为 {float(row(7,'production20')['outputJump']):.4f}、{float(row(8,'production20')['outputJump']):.4f}。一些决策分量在相邻方向剧烈跳变，网络却给出平滑曲线。方向接近并不意味着对应的 30 维真实决策接近。

以下均从零初始化，固定同一数据、学习率、损失及 5000 次 G 更新，专门比较表示与宽度。冷启动只是保证结构比较公平，不是建议每代重建模型。

{table(structure_arms)}

原 G 有 2366 个参数，加宽后为 21662；方向 one-hot 条件的 32×32 G 有 5502 个参数。one-hot 同时改变条件维度与输入层参数，因此不能宣称完全剥离了容量因素；但更大的普通宽网络仍明显不及更易区分方向的条件表示，证据不支持把问题简化为“32 个神经元太少”。这些结果支持方向间拟合干扰的重要性。

![问题 7：端点偏移、条件响应和权重分布]({OUT}/LIRCMOP7_BC_diagnosis.png)

![问题 8：端点偏移、条件响应和权重分布]({OUT}/LIRCMOP8_BC_diagnosis.png)

**5. 损失没有互相矛盾，但权重使整体拟合失衡**

当前 G 损失为 `Ladv + 10·Lendpoint + Ldelta`，两个几何项都除以每对的 `max(gap², 1e-6·D)`。令两端误差为 `eF、eI`，平均位置误差为 `m=(eF+eI)/2`，配对差误差为 `d=eI-eF`，则每对几何项等于：

`[10·||m||² + 3.5·||d||²] / max(gap², 1e-6·D)`。

真实端点能同时把两个几何项变成零，因此不是两个监督项在数学上要求互相矛盾的输出。问题在于共享参数不能同时充分拟合时，哪些点获得了主要优化权重。

末期问题 7、8 权重最高的约 10% 配对分别占总权重的 85.79%、85.59%。按 `(sum w)² / sum(w²)` 衡量权重集中度，95、92 对样本分别只有约 5.31、5.04 对的均衡权重等效量。这个量不是实际训练样本数；所有活跃配对仍然会被遍历。

继续降低原损失可以牺牲总体误差。问题 7 从 20 步延长到 1000 步，固定零噪声全批几何损失从 3287.8 降到 2465.2，而未加权端点 RMSE 从 0.1250 升到 0.1338。问题 8 同样从 1109.7 降到 782.6，RMSE 却从 0.0907 升到 0.0974。模型在优化现有目标，但现有权重允许它对整体端点作较差的折中。

去掉对抗项几乎不改变结果。原模型同一全批、零噪声的几何梯度范数约为 {float(row(7,'production20')['geometryGradientNorm']):.1f}、{float(row(8,'production20')['geometryGradientNorm']):.1f}，对抗项约为 {float(row(7,'production20')['adversarialGradientNorm']):.2f}、{float(row(8,'production20')['adversarialGradientNorm']):.2f}。这不是对所有 GAN 的结论；它说明本实现当前主要由几何回归驱动，不能把偏移主要解释成 Critic 把 G 拉歪了。

均衡权重对照仅取消批内配对间的权重差异，保留该批原逆 gap 平方权重的平均量级，避免同时把几何项整体缩小。它改善部分整体 RMSE，但没有独自解决目标空间偏移，也不是本轮批准的新损失。

**6. 持续训练应保留；预算不足只是部分原因**

{table(budget_arms)}

首训确实不足：问题 7 固定首次 12 对训练样本后，200 步的 RMSE 为 0.21330，1000 步降到 0.10524；但仍没充分拟合。后期不是简单的步数问题：问题 7 冻结 FE=30000 的数据，20 步为 0.05095，1000 步为 0.04806；在末期冻结数据上，更多更新还可能加重未加权误差。

生产模型确实保留权重、Adam 状态和更新计数。相同数据、相同随机状态可以复现真实训练；没有发现非预期重置、端点归一化错位或标签正反颠倒。当前批次从零训练 1000 步明显比热启动差，也与前一轮独立检查 Adam 重置无收益的结果一致。

“每次训练集确定”只表示这次调用的监督目标确定。四题相邻成功训练中、共同参考方向的真实目标移动 RMSE 中位数约为 0.0852、0.0748、0.0653、0.0953。模型面对的是不断移动、且局部变化不平滑的绝对坐标映射。20 步在约 90 对数据上只有约 3.33 次遍历，很难实时追上；但冻结后仍拟合不好说明不能仅归咎于目标移动。

随机噪声也不是主因。当前端点回归对同一条件的每个随机 z 都使用同一真实端点作监督，本来就倾向抑制条件内波动。对照中同时去掉训练和生成噪声，误差仅轻微变化。

**7. 决策 RMSE 降低不等于目标空间和边界标签正确**

训练到 10000 步后，方向 one-hot 的 RMSE 分别为 0.03312、0.03347；相同预算的原条件 32×32 网络为 0.13590、0.08925。但前者的候选目标 RMSE 仍为 0.23397、0.23667，生成的可行端点标签正确率仅为 6.91%、9.78%。它们不是成功的边界生成器。

![决策拟合与目标空间误差并不等比例下降]({OUT}/fit_vs_objective.png)

代码中目标函数包含 `10·sum((xj-hj(x1))²)`。对于固定的一组奇/偶变量，令真实残差为 `r`，生成残差变化为 `dr`，目标变化严格包含 `20·r·dr + 10·||dr||²`，另加 x1 的基函数变化。不同变量误差的方向、抵消与平方项都影响目标值，不能只凭一个全局决策 RMSE 推断图中距离。例如在残差为零、x1 不变的简化情形，15 个变量各误差 0.05，就会给一个目标增加 0.375。

本轮用保存的真实输出逐点核验这一恒等式，最大重构误差小于 `1.4e-15`。更接近真实决策并不保证每次有限改进都让目标误差单调下降。问题 7、8 的 one-hot 10000 步模型平均相对端点误差仍约为 2.32 个真实 pair gap，并没有达到边界对所需的局部精度。

真实可行端点位于很窄的约束边界附近；`side=1` 是模型输入标签，不是数学上的可行性保证。因而本轮没有用“全局 RMSE 降了”或“图像看上去更接近”来宣称已经修好。

**8. 建议的处理顺序与尚未证明的部分**

优先修改端点映射的建模方式：增强参考方向的区分，或使输入包含由已评价存档提供的当前局部状态，减少网络仅凭方向反复记忆整套 30 维绝对端点的负担。本轮 one-hot 是诊断工具，不能直接作为最终方案；它没有证明在线更新仅 20 步就足够。

随后处理几何损失的权重：把“绝对位置是否拟合”和“配对间隙/方向是否拟合”分别控制，对相对 gap 项限制极端权重。当前实验支持这一方向有必要研究，但尚未验证任何具体新系数或截断值。

保持持续训练，再根据上述改动后的真实拟合速度选择首训和重训预算。单独把每次训练改成从零开始、把 20 改成一个更大常数，或把 32×32 换成更宽网络，不是本轮证据支持的优先方案。

进入正式算法前，除固定训练集拟合外，还需检查原生端点与候选的目标误差、相对 pair gap 误差、标签及配对方向，最后与已有 pair-only、fallback-only 做同种子完整搜索对照。存档已持有真实端点，CGAN 必须证明它学习的结构带来额外搜索收益；目前这些离线诊断没有证明这一点。

本轮没有进行新的完整搜索或修改生产算法。固定集对照主要是两题、一个原搜索种子、一个末期状态；不能据此证明某个新结构的普遍优势。旧存档批次的结果仅用于交叉参照，主要结论由修正后数据重新验证。

**复核与成本**

- `model_summary.csv`：48 个诊断结果、损失、梯度、配对方向与目标误差。
- `LIRCMOP*_geometry.csv`：原图对应实际生成事件与正确端点插值的比较。
- `LIRCMOP*_training.csv`：1501 次训练的前后误差、目标移动和权重集中度。
- `objective_decomposition.csv`：目标误差恒等式的逐点核验。
- `protocol.json`、`verification.json`、`manifest.json`：范围、源码与产物核验。
- `reproduce_offset.m`：通过生产生成接口复现偏移，默认读问题 7 的精确重放模型。当前断言仍为红，2% 范围阈值仅用于诊断，并非新增生产验收规则。
- `extract_case.m`、`replay_case.m`、`run_comparisons.m`、`run_extended.m`、`run_factorial.m`、`run_fit_limit.m`：数据提取与对照入口；`PairBoundaryWGAN_Diagnostic.m` 是隔离副本，未加入生产路径。

所有离线真实调用使用独立问题对象，搜索 FE 始终为零；实际发生 CalObj 2,561,626 行、CalCon 859,804 行，前者包含后者内部的调用，不能相加解释成完整 FE。这些是诊断成本，不是假装零代价的额外搜索样本。目标恒等式核验与径向能量分析使用已保存结果及公式代数运算，没有反馈搜索。

最初提取问题 7、8 后，批处理由于自身路径恢复而未找到后续提取函数，其中一个 MATLAB 进程退出时还产生了崩溃日志。已修正诊断入口路径，补齐问题 5、6；问题 7、8 的已保存数据随后完成精确模型重放与最终核验。该诊断启动问题没有影响正式实验结果。
'''
(OUT / 'RESULTS.md').write_text(text)
print('RESULTS.md written from 48 verified model rows.')
