for i = 1:9
    prob = str2func(sprintf('DASCMOP%d_BC', i));
    platemo( ...
        'algorithm', @CCMO_GAN_BDG, ...
        'problem',   prob, ...
        'N',         100, ...
        'maxFE',    200000);
end

for i = 1:14
    prob = str2func(sprintf('LIRCMOP%d_BC', i));
    platemo( ...
        'algorithm', @PRBCCMO, ...
        'problem',   prob, ...
        'N',         100, ...
        'maxFE',     200000);
end
