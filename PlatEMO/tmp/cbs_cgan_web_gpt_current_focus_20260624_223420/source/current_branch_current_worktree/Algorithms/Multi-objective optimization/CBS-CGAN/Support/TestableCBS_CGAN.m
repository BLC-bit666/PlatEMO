classdef TestableCBS_CGAN < CBS_CGAN
%TESTABLECBS_CGAN Test helper that captures CBS_CGAN boundary options.

    methods
        function [BMem,Diag] = updateBoundaryMemory(Algorithm, ...
                PrevBMem,Population1,Offspring1,Population2,Offspring2, ...
                W,Options) %#ok<INUSD>
            setappdata(0,'CBS_CGAN_DefaultRadiusProbe', ...
                Options.pairNeighborRefRadius);
            BMem = [];
            Diag = struct( ...
                'bmem_count',0, ...
                'boundary_count',0, ...
                'finite_gap_count',0, ...
                'inf_gap_count',0, ...
                'median_gap',NaN, ...
                'max_gap',NaN);
        end
    end
end
